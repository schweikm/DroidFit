<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="marcschweikert.com.droidfit">

    <!--    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NewAccountActivity -->
        <activity
            android:name=".NewAccountActivity"
            android:label="@string/title_newAccount"
            android:windowSoftInputMode="adjustResize|stateVisible"></activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/title_main"
            android:windowSoftInputMode="adjustResize|stateVisible"></activity>

        <!-- DroidFitActivityActivity -->
        <activity
            android:name=".DroidFitActivityActivity"
            android:label="@string/title_new_activity"
            android:windowSoftInputMode="adjustResize|stateVisible"></activity>
    </application>

</manifest>
